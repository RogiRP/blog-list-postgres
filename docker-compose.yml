services:
  postgres:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
      POSTGRES_DB: blog_list
    ports:
      - 5432:5432
    volumes:
      - blog_list_pg_data:/var/lib/postgresql/data

volumes:
  blog_list_pg_data:
